document.addEventListener('DOMContentLoaded', () => {const searchInput = document.getElementById('searchInput');const searchButton = document.getElementById('searchButton');const performSearch = () => {const query = searchInput.value.trim();if (query) {// Encode the query to handle special charactersconst encodedQuery = encodeURIComponent(query);// Google search URLwindow.location.href = `https://www.google.com/search?q=${encodedQuery}`;} else {// Optionally, provide feedback if the search input is emptyalert('Oops! Please type something to search.');}}; // Attach event listener to the search buttonsearchButton.addEventListener('click', performSearch);// Attach event listener for 'Enter' key on the input fieldsearchInput.addEventListener('keypress', (event) => {if (event.key === 'Enter') {performSearch();}});});